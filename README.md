Express React Node Template
===========================

# Install

`mkdir app` **app - your project name** \
`cd app` \
`git clone git@github.com:BykovVik/ERN-Template.git` \
`yarn install` \
`cd frontend` \
`yarn install` \
`cd ..` \
`cd backend` \
`yarn install`